import 'package:flutter/cupertino.dart';

double logoHeight = 100;
double logoOffset = 120;
double categoryHeight = 100;

Duration animationDuration = const Duration(milliseconds: 175);

EdgeInsets commonEdgeInstes = const EdgeInsets.all(10);
EdgeInsets largeEdgeInsets = const EdgeInsets.all(20);
EdgeInsets smallEdgeInstes = const EdgeInsets.all(5);
EdgeInsets commonBottomEdgeInsets = const EdgeInsets.only(bottom: 10);

BorderRadius commonRadius = const BorderRadius.all(Radius.circular(10));
BorderRadius smallRadius = const BorderRadius.all(Radius.circular(5));
